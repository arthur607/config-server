spring:
  #config:
    #import: configserver
  application:
    name: greeting-service
greeting-service:
  greeting: "ola"
  default-value: "hml"
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'

